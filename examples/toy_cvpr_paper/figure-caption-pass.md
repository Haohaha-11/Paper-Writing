# Figure Caption Pass

Use `skills/figure-caption-writer/SKILL.md`.

## Figure 1

Purpose: method overview.

Caption requirements:

- name the input, encoder, boundary token branch, and output segmentation map;
- indicate which arrows are training-only;
- avoid claiming performance improvement in the architecture figure.

## Figure 2

Purpose: qualitative comparison.

Caption requirements:

- identify dataset split and image selection rule;
- state the compared methods;
- point to boundary regions without implying universal superiority;
- include failure examples if space allows.

## Table 1

Purpose: main benchmark comparison.

Caption requirements:

- define all metrics;
- state whether higher or lower is better;
- identify backbone and training data;
- mention that values must come from the same protocol.
