# Section Plan

## Abstract

Use `abstract-writer`. State low-label calibration problem, method mechanism, measured result type, and limitation that evidence is restricted to selected benchmarks.

## Introduction

Use `introduction-writer`. Build the argument from label scarcity to overconfident representations to prototype uncertainty.

## Related Work

Use `related-work-writer` and `novelty-scout`. Required buckets:

- contrastive learning under limited labels;
- prototype learning;
- calibration and uncertainty;
- low-label benchmark protocols.

## Method

Use `method-writer`. Define notation before the objective. Separate the prototype uncertainty objective, sampling policy, and training algorithm.

## Experiments

Use `experiments-writer`. Report datasets, label fractions, baselines, metrics, seeds, and compute. Do not state superiority until all baselines are verified.

## Results

Use `results-writer`. Pair every major table with a narrow takeaway and an uncertainty note.

## Limitations

Use `limitations-ethics-writer`. Note domain limits, compute assumptions, and calibration failure cases.
